import { GetCandidateStatus } from './get-candidate-status';

describe('GetCandidateStatus', () => {
  it('should create an instance', () => {
    expect(new GetCandidateStatus()).toBeTruthy();
  });
});
