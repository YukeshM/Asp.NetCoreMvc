import { UpdateRoundInfoByCandidateId } from './update-round-info-by-candidate-id';

describe('UpdateRoundInfoByCandidateId', () => {
  it('should create an instance', () => {
    expect(new UpdateRoundInfoByCandidateId()).toBeTruthy();
  });
});
