import { UpdateCandidateRoundInformation } from './update-candidate-round-information';

describe('UpdateCandidateRoundInformation', () => {
  it('should create an instance', () => {
    expect(new UpdateCandidateRoundInformation()).toBeTruthy();
  });
});
