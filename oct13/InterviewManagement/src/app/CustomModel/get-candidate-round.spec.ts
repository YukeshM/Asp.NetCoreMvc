import { GetCandidateRound } from './get-candidate-round';

describe('GetCandidateRound', () => {
  it('should create an instance', () => {
    expect(new GetCandidateRound()).toBeTruthy();
  });
});
